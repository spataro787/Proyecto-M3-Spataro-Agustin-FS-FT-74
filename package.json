{
  "name": "gandalf-ai-chat",
  "version": "1.0.0",
  "description": "Single Page Application con IA basada en Google Gemini",
  "private": true,
  "type": "module",

  "scripts": {
    "dev": "http-server . -p 5173 -c-1",
    "start": "http-server . -p 3000 -c-1",
    "build": "echo \"Build OK\"",
    "test": "vitest run",
    "test:watch": "vitest"
  },

  "dependencies": {
    "@google/generative-ai": "^0.21.0"
  },

  "devDependencies": {
    "http-server": "^14.1.1",
    "vitest": "^1.6.1"
  },

  "engines": {
    "node": ">=18"
  }
}